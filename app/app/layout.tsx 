import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "ZeroFounder",
  description: "An AI co-founder companion that helps solo builders turn rough ideas into launch-ready plans with persistent memory on 0G.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
